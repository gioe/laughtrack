'use client';

import Image from "next/image";

interface LogoProps {
    onClick: () => void;
}

const Logo: React.FC<LogoProps> = ({onClick}) => {
    return (
        <div></div>
    )
}

export default Logo;