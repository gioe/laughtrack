package app.laughtrack.android.core.ui.theme

import androidx.compose.material3.Typography

/**
 * Default Material 3 type scale. The custom brand face is deliberately NOT wired
 * here: the iOS/web Gilroy font is unlicensed and must be replaced before any
 * branded typography ships (see the iOS font-swap work). Until a licensed face is
 * chosen, the platform default keeps the scaffold legally clean.
 */
val LaughTrackTypography = Typography()
