<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <!-- POST_NOTIFICATIONS gates FCM notifications on Android 13+ (requested at
         runtime from MainActivity). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Coarse/fine location power the Home "Use location" ZIP resolution
         (FusedLocationProvider + Geocoder), requested at runtime from HomeScreen. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".LaughTrackApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LaughTrack">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.LaughTrack">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Custom-scheme deep links + native OAuth redirect (laughtrack://). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="laughtrack" />
            </intent-filter>
        </activity>

        <service
            android:name="app.laughtrack.android.core.playback.PodcastPlaybackService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
        </service>

        <!-- FCM message handler: token refresh + comedian-arrival notifications. -->
        <service
            android:name=".push.LaughTrackMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Channel FCM uses for notification-type messages handled while backgrounded. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="comedian_arrivals" />

        <!-- Strip Sentry's auto-init ContentProviders. The SDK ships these in its
             manifest; SentryInitProvider.onCreate() calls Sentry.init() before
             Application.onCreate and throws "DSN is required" on any build without
             a DSN (debug/CI), crashing the process at startup. We init Sentry
             manually and only when a DSN was injected
             (LaughTrackApplication.initSentry), so auto-init must never run.
             Removing the providers outright is deterministic — the softer
             `io.sentry.auto-init=false` meta-data was still read as `true` by some
             app-under-test launches (a startup race), which flaked the
             instrumented-test job. With the providers gone there is nothing to
             auto-init. -->
        <provider
            android:name="io.sentry.android.core.SentryInitProvider"
            android:authorities="${applicationId}.SentryInitProvider"
            tools:node="remove" />
        <provider
            android:name="io.sentry.android.core.SentryPerformanceProvider"
            android:authorities="${applicationId}.SentryPerformanceProvider"
            tools:node="remove" />
    </application>

</manifest>
