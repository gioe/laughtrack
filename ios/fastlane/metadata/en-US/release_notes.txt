New Features
• [TASK-2738] iOS: add status-bar scrim to DetailChromeBar so scrolled detail content fades out before reaching the clock. Canvas-tone gradient is opaque behind the status bar and fades to clear past the chrome buttons, escaping the safe-area inset to cover the physical top edge. Both criteria co-located: the scrim lives in the shared chrome, so show, comedian, club, and podcast detail all get it from this one change (verified via simulator screenshots on all four screens).

Bug Fixes
• [TASK-2740] iOS: restore swift test green by realigning four stale test suites with current production code. (1) ShowRowTests price-label assertions: production intentionally moved to 'From dollars-X' compact format in bc198d63e -- update from range to From-prefix. (2) HomeContentSectionTests: HomePopularClubCard sourceBlock end-marker fixed to posterFallback (fallbackArtwork lives only in HomeShowsTonightHeroCard above it, causing NSCocoaErrorDomain 259 when sourceBlock cannot locate the marker after the start position); hero card scaledToFill assertion updated to match a8f656618's intentional marquee-styling change; copy assertion updated from removed Comics on the rise this week to current Comedians drawing crowds title. (3) SearchFavoriteRowLayoutTests: drop assertions for an unimplemented trailingAccessory refactor; verify the actual HStack plus Button plus FavoriteButton layout that shipped. (4) TonightNearYouLoaderTests fixture adds the hosts field that TASK-2659 made required on PodcastDetail so JSONDecoder no longer fails and the loader gets past detail decode. Bundling all three criteria (8930, 8931, 8932) into one commit because the swift test exit 0 gate (8930) cannot be verified piecemeal -- a partial fix leaves the suite red and blocks the next tusk commit's test_command. Each of 8931 (price-label format) and 8932 (NSCocoaErrorDomain 259 fixture) is independently verifiable within this commit's diff. swift test now passes 496/496.
• [TASK-2741] Fix PillSheetTrigger doc comment to say tone shifts to .accent when active, matching the code (it claimed .selected, which no longer exists)
• [TASK-2735] iOS: club detail hero falls back to imageUrl when heroImageUrl is empty so Comedy Cellar's logo replaces the generic building glyph. Both criteria co-located: the helper fix and its tests (imageUrl fallback + both-empty nil) are one inseparable change in ClubDetailHeroPresentation.

Improvements
• [fastlane] Bump build to 23
• [TASK-2749] iOS: MarqueeHero derives status-bar clearance from the key window's top safe-area inset instead of a hardcoded 60pt. The detail views ignore the top container safe area on their scroll containers, so SwiftUI propagates a zero inset to the hero and a local GeometryReader cannot read the real value; the key-window read (same pattern as OAuthSessionRunner) adapts to the actual device. Calibration: measured iPhone 17 sim inset is 62pt, so offset = inset - 2 reproduces the prior 60pt exactly on the Dynamic Island baseline (verified pixel-identical screenshots: eyebrow/title/chrome rows unchanged), while iPhone 16e (47pt notch inset) hero content lifts 15pt to sit 1-2pt below the chrome button bottom, matching the Dynamic Island relationship. Fallback 62 covers previews and the macOS swift test build, preserving the old 60pt total. Bundling both criteria: the inset-derived offset and its screenshot verification are one inseparable file-local change. swift test 496/496 green.