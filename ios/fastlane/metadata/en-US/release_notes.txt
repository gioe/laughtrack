New Features
• [TASK-3276] Add club shows API contract and clients
• [TASK-3273] Regenerate iOS + Android OpenAPI clients in lockstep for /podcasts/search (convention #220). iOS adds Operations.SearchPodcasts + PodcastSearchItem/Response (additive, drift check passes); Android adds PodcastsApi.searchPodcasts + PodcastSearchItem/Response models (drift check passes). MeData.kt zipCode/nearbyDistanceMiles drop to non-null is pre-existing Android drift from a prior spec change that regenerated iOS but not Android; this full regen normalizes it
• [TASK-3273] Add GET /podcasts/search to OpenAPI spec with PodcastSearchItem/Response schemas. Params (q, sort, page, size, includeEmpty) match the actual apps/web/app/api/v1/podcasts/search route; omits filters/X-Timezone since that route reads neither (interceptor sets timezone globally). Item shape mirrors PodcastDetailPodcast
• [TASK-3285] iOS: add per-flow OAuth state nonce to close the native-callback login-CSRF gap (mirrors Android TASK-3271). AuthManager mints a 32-byte base64url nonce, embeds it on the callbackUrl via AuthRouteConfiguration.signInURL(for:state:), and rejects token callbacks whose returned state is missing/mismatched before storing tokens. Grouped 10722 (emit+embed state) and 10723 (reject mismatch before storing) — both are one inseparable change to AuthManager.signIn + the URL builder. Verified via swift test (20 logic tests pass incl. mismatch/missing/round-trip)
• [TASK-3162] Add NotificationsAnalyticsEvents and instrument notification center (viewed + card_tapped); enum and its callsites are one change
• [TASK-3064] Add notification center screen, route, model + tests
• [TASK-3063] Add notification ops to OpenAPI spec + regenerate iOS client in lockstep
• [TASK-2919] Add cache version-invalidation + decode-failure tests

Bug Fixes
• Fix iOS auth test failures
• Fix Comedy Chateau SeatEngine ticket links
• Fix iOS search location results
• [TASK-3290] Switch NotificationListResponse/NotificationsSeenResponse data.lastSeenAt to 3.1 union nullable form in openapi.json. Both were required + 3.0 'nullable: true', which openapi-generator 7.11.0 (Android) ignores, emitting non-null fields that throw on a null lastSeenAt (user who never opened the notification center). Use type: [string, null] (per convention #230, same fix as TASK-3286 MeData).
• [TASK-3285] Apply review fix: clear the OAuth pendingState nonce via defer so it is reset on every signIn exit path (success, state-mismatch, error), not just success. Benign per review #3123 (each signIn overwrites it before reading) but removes the asymmetric-lifecycle smell. swift test 18/18 still pass
• [TASK-3064] Apply review fixes: gate mark-seen on loaded state, persist row dots, shared relative formatter
• [TASK-3154] Fix AuthLoadingView LoadingMarqueeBulbs type-check timeout by hoisting arithmetic into typed lets (build + no-visual-change, one inseparable fix)

Improvements
• Update show detail alternate rows
• Remove show detail countdown badge
• Regenerate iOS Xcode project
• Update mobile UI and tusk tooling
• [TASK-3286] Switch MeData.zipCode/nearbyDistanceMiles to 3.1 union nullable form in openapi.json. They were required + 3.0 'nullable: true', which openapi-generator 7.11.0 (Android) ignores, emitting non-null fields that throw on a null /me response. Use type: [type, null] to match PodcastSearchItem style.
• [TASK-3064] Turn profile button into a menu with unread badge wired to /me notificationsUnreadCount
• [fastlane] Bump build to 30
• iOS: handle rich notification deeplinks
• [TASK-2920] Clarify in-memory DataCache vs disk-backed PersistentMainPageCache in cache-bleed doc
• [TASK-2919] Stamp PersistentMainPageCache entries with build version; invalidate stale/undecodable blobs
• iOS: silently refresh stored ZIP on app activation for accurate location pushes