{
  "buildCommand": "cd apps/web && : \"${DIRECT_URL:?DIRECT_URL must be set to the direct (non-pooled) Neon URL; prisma migrate deploy hangs on advisory locks over the pooled DATABASE_URL}\" && prisma migrate deploy && npm run build",
  "framework": "nextjs",
  "installCommand": "npm ci --prefix apps/web",
  "outputDirectory": "apps/web/.next"
}
