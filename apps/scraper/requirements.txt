# Core Dependencies
requests>=2.33.1       # HTTP library for synchronous requests
httpx[http2]>=0.28.0   # HTTP/2 client for APNs push notification delivery
cryptography>=47.0.0   # ES256 signing for APNs provider authentication tokens
urllib3>=2.7.0         # HTTP backend for requests; floor pinned to patch CVEs (decompression bomb, redirect header forwarding)
curl-cffi>=0.5.0       # Async HTTP client with Chrome TLS/JA3 fingerprinting
asynciolimiter>=1.0.0  # Async rate limiting for HTTP requests
beautifulsoup4>=4.12.0 # HTML/XML parsing for web scraping
feedparser>=6.0.11     # RSS/Atom parsing for podcast episode ingestion
psycopg2-binary>=2.9.5 # PostgreSQL adapter for Python
pydantic>=2.0.0        # Data validation and settings management
loguru>=0.7.0          # Advanced logging library
pytz>=2023.3           # Timezone handling for show dates
python-dotenv>=1.0.0   # Environment variable loading from .env files

# Optional: Playwright JS fallback (lazy-loaded; only used when bot-block detected)
# After installing, run: playwright install chromium
playwright>=1.40.0     # Headless browser for JS-rendered pages

# Web Scraping & Data Processing
Pillow>=12.2.0         # Image processing; floor pinned to patch PSD OOB-write and FITS GZIP decompression-bomb CVEs
PyYAML>=6.0            # YAML file parsing for configuration
python-dateutil>=2.8.0 # Date parsing and manipulation utilities

# Development & Testing
pytest>=7.4.0          # Testing framework
pytest-asyncio>=0.21.0 # Async testing support
pyclean>=2.2.0         # Utility for cleaning Python cache files

# Optional Dependencies (for visualization and analysis)
# Install with: pip install -r requirements.txt -r requirements-optional.txt
# streamlit>=1.45.1     # Web dashboard for metrics visualization
# plotly>=6.1.1         # Interactive plotting library
# pandas>=2.2.3         # Data analysis and manipulation

# Notification distance filtering
pgeocode>=0.4.0         # Zip code to lat/lon lookup for notification distance filtering
numpy>=1.24.0           # Required by pgeocode
pandas>=2.0.0           # Required by pgeocode

# Gmail API (email inbox client)
google-auth>=2.0.0             # OAuth2 authentication for Google APIs
google-api-python-client>=2.0.0 # Google API client library (Gmail, etc.)

# Internal Libraries
gioe-libs @ git+https://github.com/gioe/python-libs.git  # Shared infrastructure utilities

# System/Build Dependencies
wheel                   # Python wheel package format
pathlib>=1.0.1         # Path handling (usually built-in in Python 3.4+)
