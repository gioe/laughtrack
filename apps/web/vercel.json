{
  "buildCommand": ": \"${DIRECT_URL:?DIRECT_URL must be set to the direct (non-pooled) Neon URL; prisma migrate deploy hangs on advisory locks over the pooled DATABASE_URL}\" && prisma migrate deploy && npm run build",
  "crons": [
    {
      "path": "/api/cron/youtube-websub-renewals",
      "schedule": "0 9 * * *"
    },
    {
      "path": "/api/cron/cleanup-ticket-purchase-click-events",
      "schedule": "30 9 * * *"
    }
  ],
  "framework": "nextjs",
  "installCommand": "npm ci",
  "outputDirectory": ".next"
}
